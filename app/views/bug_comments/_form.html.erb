<%= form_for(@bug_comment, :url=>project_bug_list_bug_post_bug_comment_path(params[:project_id], params[:bug_list_id], params[:bug_post_id])) do |f| %>
  <% if @bug_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug_comment.errors.count, "error") %> prohibited this bug_comment from being saved:</h2>

      <ul>
      <% @bug_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <%= f.hidden_field :bug_post_id, :value=>params[:bug_post_id] %>
  <%= f.hidden_field :user_id, :value=>session[:user_id] %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <%= f.label :img_url %><br />
    <%= f.text_field :img_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

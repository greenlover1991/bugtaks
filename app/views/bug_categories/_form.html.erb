<%= form_for(@bug_category) do |f| %>
  <% if @bug_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bug_category.errors.count, "error") %> prohibited this bug_category from being saved:</h2>

      <ul>
      <% @bug_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
